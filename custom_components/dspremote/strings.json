{
  "config": {
    "step": {
      "user": {
        "title": "Connect to dspremote",
        "description": "Enter the host and port of your running dspremote API server.",
        "data": {
          "host": "Host",
          "port": "Port",
          "ssl": "Use HTTPS",
          "use_websocket": "Enable websocket updates",
          "prefix": "Read prefix",
          "selected_paths": "Selected field paths/patterns (comma or newline separated)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Select dspremote fields",
        "description": "Only matching fields will be created as entities. Supports exact paths and wildcards (for example /outputs/*/gain). Leave empty to create no field entities.",
        "data": {
          "selected_paths": "Selected field paths/patterns (comma or newline separated)"
        }
      }
    }
  }
}
